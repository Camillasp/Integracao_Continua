{
  "description": "",
  "devDependencies": {
    "cypress": "^13.6.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open":"npx cypress open", 
    "cypress: run": "npx cypress run", 
    "cypress: dashboard": "",
    "cypress: report": "",
    "npm run npm": "npm install"
  
  }
}

